name: Deploy Functions

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'main.py'
      - 'clients/**'
      - 'models/**'
      - 'services/**'
      - 'handlers/**'
      - 'repo/**'
      - 'requirements.txt'
      - 'terraform/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v7

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SA }}

      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "~1.5"

      - name: Terraform init
        working-directory: terraform
        run: terraform init

      - name: Terraform apply
        working-directory: terraform
        env:
          TF_VAR_project_id: bens-project-462804
          TF_VAR_people_db_password: ${{ secrets.TF_VAR_PEOPLE_DB_PASSWORD }}
          TF_VAR_people_api_token: ${{ secrets.TF_VAR_PEOPLE_API_TOKEN }}
          TF_VAR_people_sync_token: ${{ secrets.TF_VAR_PEOPLE_SYNC_TOKEN }}
          TF_VAR_hubspot_owner_id: ${{ vars.HUBSPOT_OWNER_ID }}
          TF_VAR_deployer_sa: ${{ secrets.GCP_DEPLOYER_SA }}
        run: terraform apply -auto-approve
